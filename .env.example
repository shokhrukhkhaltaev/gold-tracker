# Backend
PORT=3001
NODE_ENV=development
DATA_DIR=./data

# CORS — comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:5173,http://localhost

# Cron schedule (default: 04:00 UTC = 09:00 Tashkent)
# Format: minute hour day month weekday
CRON_SCHEDULE=0 4 * * *
